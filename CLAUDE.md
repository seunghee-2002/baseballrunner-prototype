# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 현재 상태

**웹 프로토타입이 구현되어 있다.** 빌드 도구·패키지 매니저·테스트 러너는 쓰지 않는다.

```
index.html      화면 골격 (세로 9:16), 4분할 입력 영역, HUD, 오버레이
style.css       HUD / 연출 / 오버레이 스타일
js/data.js      §11 매핑 · §40 장애물 데이터 · §14 패턴 풀 · §32 난이도 예산 · 규칙 상수
js/scene.js     three.js 렌더링 (프리미티브만). distance -> 3D 좌표 환산
js/ui.js        HUD 갱신, 결과 화면, 오실레이터 효과음
js/game.js      상태 머신 (§39) 과 모든 게임 규칙
```

* **실행**: `index.html` 을 브라우저로 연다. 서버 불필요. three.js 는 CDN(UMD, 전역 `THREE`)에서
  로드하므로 인터넷 연결이 필요하다. 로드 실패 시 화면에 안내가 뜬다.
* **빌드/린트/테스트 명령 없음.** 문법 확인은 `node --check js/*.js`.
* 규칙 검증이 필요하면 DOM 과 `Scene3D` 만 스텁으로 대체하고 `js/data.js` · `js/ui.js` ·
  `js/game.js` 를 node 에서 그대로 구동하면 된다 (`vm.runInThisContext` 로 로드하면
  최상위 `var` 가 전역이 되어 브라우저와 동일하게 동작한다).

모든 스크립트는 classic script 다. `type="module"` 로 바꾸면 `file://` 직접 실행이 깨진다.

`기획안.md` 는 모든 설계 판단의 근거다. 작업 전에 관련 섹션을 읽고, 근거를 밝힐 때 **§ 번호로
참조**한다. **코드와 문서가 충돌하면 코드가 우선이다.**

### 기획안 자체의 불일치 — 이미 결론이 났다

§9 는 "좌상 입력 = 오른쪽 상체 회피"라고 적었지만, 이는 §11 의 정답 표와 어긋난다
(좌상 위험의 정답이 우상 입력인데 그 입력이 왼쪽 회피가 되어버린다).
**§11 표가 우선**이고, 액션 명칭은 "입력한 방향으로 몸을 피한다"로 정합화했다
(`js/data.js` 의 `DANGER_MAP` / `ACTION_NAME`). 되돌리지 않는다.

## 이 게임이 무엇인가 (§2, §43, §46)

한 줄: **타격으로 루타의 가능성을 만들고, 주루 액션으로 그 가능성을 실현한다.**

핵심 차별점은 **타격 결과가 곧 최종 결과가 아니라는 것**이다 (§3).

```
타격 판정 → CHANCE(잠재 결과) 생성 → 주루 회피 → 최종 루타 확정
```

예: `2B CHANCE` + 주루 PERFECT → `3B`.  `2B CHANCE` + 주루 실패 → `1B`.

이 4단계 연결이 프로토타입의 존재 이유다. 어떤 이유로도 이 사슬을 끊거나,
타격 시점에 결과를 확정하는 구조로 만들지 않는다 (§43).

상태 머신 (§39):

```
TITLE → PITCH → BAT → BAT_RESULT → CHANCE → RUNNING ⇄ BASE_PASS → RESULT → PITCH
                        ↑   ↓                   ↓
                     FOUL 재투구               OUT → RESULT
```

**주루는 베이스 하나마다 끊어서 달린다.** `RUNNING` 한 번 = 베이스 한 구간이고,
`BASE_PASS` 를 거쳐 다음 구간으로 이어진다. 달릴 구간 수는 **현재 CHANCE** 이므로
주루 중 승격하면 구간이 하나 늘고, 하락하면 거기서 멈춘다.
**최종 결과는 CHANCE 가 아니라 실제로 밟고 지나간 베이스 수(`G.base`)** 다.

시간은 루타에 따라 크게 갈린다: 1B 약 3초 / 2B 약 8초 / 3B 약 13초 / HR 약 18초.
§4-2 의 "주루 7~12초" 를 3B 이상에서 넘기는데, 이는 "1·2루를 지나는 과정을 보여달라" 는
사용자 요구를 받은 결과다. 길이를 줄이려면 `LEG_BUDGET` 을 낮춘다.
**레벨은 구간의 길이가 아니라 밀도(속도·반응시간·간격)만 올린다.** 뒤 구간이 험해지는 것은
`LEG_DIFFICULTY_BIAS` 로 어려운 패턴 쪽에 가중치를 주어 표현한다.

## 반드시 지켜야 하는 규칙

수치는 튜닝해도 되지만 아래 원칙은 바꾸지 않는다.

- **주루에 자유 이동이 없다.** 캐릭터는 자동 전진하고, 플레이어가 조작하는 것은 **회피 행동뿐**이다.
  3레인 좌우 이동 방식으로 되돌리지 않는다 (§7-2).
- **입력은 화면 4분할 터치.** 좌상/우상/좌하/우하 영역 터치가 각각 상체 회피 2종·다리 회피 2종에
  대응한다. 큰 버튼 그래픽은 필수가 아니다 (§8, §9).
- **상체와 하체의 정답 규칙이 서로 반대다.** 이 비대칭은 사용자가 지시한 **의도적인 혼란 요소**다.
  기획안 §11 은 넷 다 반대쪽이었으나 하단만 반전했다. 되돌리지 않는다:

  | 위험 위치 | 위험 유형 | 정답 입력 | 규칙 |
  | -- | -- | -- | -- |
  | 좌상 | 날아오는 공 | 우상 | **반대쪽** 으로 몸을 피한다 |
  | 우상 | 날아오는 공 | 좌상 | **반대쪽** 으로 몸을 피한다 |
  | 좌하 | 수비수 태클 | 좌하 | **같은 쪽** 다리를 들어 넘긴다 |
  | 우하 | 수비수 태클 | 우하 | **같은 쪽** 다리를 들어 넘긴다 |

  이 표의 구현체는 `js/data.js` 의 `DANGER_MAP` 하나뿐이다. 분기를 만들지 않는다.

- **장애물은 데이터다** (§40). `Type / SpawnPosition / RequiredInput / MoveSpeed / ReactionTime /
  Reward / Penalty` 를 가진 데이터로 정의하고, 장애물 종류별 if/else 분기를 만들지 않는다.
- **장애물 접근은 거리값 하나로 계산한다** (§25). 실제 3차원 좌표 계산 대신 `Distance: 100 → 0`
  방식으로 접근시킨다. 3D든 가짜 3D든 원리는 동일하며, 프로토타입에서는 복잡한 3D 애니메이션을
  만들지 않는다 (§26).
- **완전 랜덤 생성 금지.** 패턴 풀 + 가중 랜덤을 쓰고 (§14), 한 주루 구간에 **난이도 예산**을
  배정해 패턴을 조합한다 (§32: 쉬움 1점 / 보통 2점 / 어려움 3점, 구간 총합 고정).
- **"나쁜 랜덤"은 버그로 취급한다** (§13-3). 화면 밖 갑작스러운 등장, 지나치게 짧은 대응 시간,
  시야 밖 충돌, 입력으로 해결 불가능한 상황 — 전부 금지. 운은 *상황을 결정*하고
  실력이 *상황을 해결*한다 (§13-1). 목표 비율은 실력 70 / 운 30 (§31).
- **실패 이유가 눈에 보여야 한다** (§37). "왜 맞았지?"가 나오면 조작 체계 자체의 실패다.
- **실패 연출은 짧게.** OUT 후 긴 연출 없이 즉시 결과 화면으로 간다 (§36).

## 기획안이 열어뒀던 항목 — 사용자가 결정했다

기획안이 두 안을 병기했던 지점들이다. 아래로 확정됐으니 다시 묻지 않는다.

- **주루 결과 계산 방식 = 연속 성공 게이지 (§18).** §17 의 성공 횟수 표는 쓰지 않는다.
  게이지 0~100, 시작 50. PERFECT +25 / SUCCESS +15, 연속 성공마다 배율 1.0→1.5.
  오답 -35 / 무반응 충돌 -45 (무반응이 더 무겁다 — 틀리더라도 반응하는 쪽이 낫게).
  100 도달 시 CHANCE 1단계 상승, 0 미만이면 1단계 하락. 상수는 `js/data.js` 의 `RUN` 에 있다.
- **HR = 포함.** 1B/2B/3B/HR 4단계다. HR 은 **3B 에서 게이지를 다시 끝까지 채워야만** 나오고,
  3B 이후에는 게이지 획득이 절반이 된다 (`RUN.HR_GAIN_RATIO`). §5-3 의 "상위 특별 결과" 를
  지키기 위한 문턱이다. HR 도달 후에도 남은 구간에서 하락할 수 있다.
- **3D 사용 범위 = three.js 실제 3D, 단 프리미티브 도형만 (§26).**
  캐릭터 3D 애니메이션은 만들지 않는다. 회피는 기울임·다리 들기 트윈으로만 표현한다.

### 회피 판정은 "충돌까지 남은 시간" 기준이다

`remain = distance / speed` 가 `RUN.PERFECT_WINDOW`(0.20초) 이하일 때 PERFECT,
그 전이면 SUCCESS, 넘기면 TOO LATE 다. **일찍 누를수록 좋은 구조로 되돌리지 않는다** —
그러면 눈에 보이는 충돌과 판정이 반대로 붙어 손이 납득하지 못한다(실제로 그 버그가 있었다).
같은 이유로 `placeObstacle` 의 충돌 지점은 `z = 0`(플레이어 몸 중심)이어야 한다.

### 타격은 타이밍 + 코스 두 축이다

공은 스트라이크존 **4칸 중 하나**로 들어오고, 주루와 같은 4분면 입력으로 **그 칸을 쳐야** 한다.
아무 데나 탭하면 되는 구조로 되돌리지 않는다. SPACE 는 오버레이를 넘기는 데에만 쓴다.

| 타이밍 | 코스 | 결과 |
| -- | -- | -- |
| ≤ 0.230s | 정확한 칸 | JUST(≤0.030) / PERFECT(≤0.070) / GREAT(≤0.140) / GOOD |
| ≤ 0.230s | 인접한 칸 | 빗맞은 안타 — GOOD(1B) 고정 |
| ≤ 0.230s | 대각선 반대 | **FOUL** — 같은 타석에서 재투구 |
| > 0.230s / 무스윙 | — | MISS → OUT |

파울은 `FOUL_LIMIT`(2회)까지만 재투구하고 3번째는 헛스윙으로 친다. 무한 반복 방지다.
JUST 는 3B 로 시작하면서 게이지를 60 에서 출발시킨다 — HR 사정권에 놓는 보상이다.

### 베이스는 공짜로 밟을 수 없다

한 구간에서 **단 하나도 성공하지 못하면 그 베이스 앞에서 태그아웃**이다 (`G.legSuccess === 0`).
이 규칙이 없으면 1루 구간(장애물 1개)을 전부 실패하고도 1루타를 받는다 — 실제로 그 버그가 있었다.
마찬가지로 하락으로 `chance <= base` 가 되면 그 구간을 완주해도 다음 베이스는 포기하고
이미 밟은 베이스에서 멈춘다. 그래야 CHANCE 하락에 의미가 생긴다.

여전히 확정되지 않은 것: 없음. 새 항목이 생기면 여기에 적는다.

## 만들지 않는 것 (§27, §44)

핵심 재미가 검증되기 전에 만들면 프로젝트가 비대해진다. 요청받지 않은 한 손대지 않는다.

실제 경기 규칙 · 볼/스트라이크 · 투수 AI · 정교한 수비 AI · 베이스 이동 AI · 다수 주자 ·
수비 포지셔닝 · 야구 스탯 · 선수 성장 · 커스터마이징 · 스킨 · 장비 · 상점 · 재화 · 뽑기 ·
강화 · 랭킹 · 온라인 · 스테이지 선택 · 실제 야구장 제작 · 복잡한 메타 시스템.

§35 의 주루 중 STOP/RUN 선택(더 뛸 것인가)은 매력적이지만 **첫 프로토타입에는 넣지 않는다**고
명시되어 있다. §34 의 장애물/액션 확장도 마찬가지로 이후 과제다.

## 무엇을 먼저 검증하는가 (§29, §30)

**타격보다 주루 시스템 검증이 우선이다** (§46). 가장 중요한 질문은
"4방향 대응 주루가 실제로 재미있는가?"이므로, 처음부터 야구 게임처럼 만들면 안 된다.

우선순위: ① 공 좌상/우상 → 반대쪽 상체 회피 ② 수비수 좌하/우하 → 반대쪽 다리 회피
③ 타격 결과가 1B/2B/3B를 결정하는 구조.

MVP 필수 목록은 §27, 난이도 단계는 §15, 스테이지 예시는 §33 에 있다.

성공 기준은 "작동한다"가 아니다 (§30 Q1–Q6). 특히 **Q5 — "2B CHANCE가 3B가 될 것 같을 때
욕심이 생기는가"** 가 핵심 리텐션 지표다. 기능 구현 후 이 감정이 발생하는지를 기준으로 판단한다.
