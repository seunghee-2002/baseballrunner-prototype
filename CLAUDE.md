# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 현재 상태

**LAN 1v1 PvP 웹 프로토타입이 구현되어 있다** (`새아이디어.md` 기반). 빌드 도구·패키지 매니저·테스트 러너는 쓰지 않는다.
이전 싱글플레이 버전은 `legacy/` 에 수정 없이 보존되어 있다 (`legacy/index.html` 을 열면 그대로 돈다).

```
index.html      화면 골격 (세로 9:16), 4분할 입력, HUD, 피버 버튼, 수비 손패, 오버레이
style.css       HUD / 손패 / 연출 / 오버레이 스타일
server.js       LAN 서버. Node 내장 모듈만. 정적 파일 + WebSocket(/ws) + 방 하나(선착 2명)
js/data.js      DANGER_MAP · 장애물(§40) · 패턴 풀(§14) · 예산(§32) · 카드 8종 · 규칙 상수   (서버 공용)
js/match.js     MatchCore — 초/말·아웃·점수·공수 교대·승패. 중계 규칙                     (서버 공용)
js/net.js       NetLink(WebSocket) / LocalLink(연습 모드: 페이지 안에서 MatchCore 구동)
js/attack.js    AttackSim — 타격·주루·회피 판정·피버·아웃 (DOM 없음) / AttackSide — 통신 껍데기
js/defense.js   DefenseState — 투구 2단계·덱·코스트·카드 사용 가능 판정 / DefenseSide — 통신 껍데기
js/bot.js       연습용 확률 봇 (BotDefender / BotBatter)
js/scene.js     three.js 렌더링 (프리미티브만). 스냅숏 distance -> 3D 좌표
js/ui.js        HUD · 손패 · 오버레이 · 오실레이터 효과음
js/game.js      로비 → 매치 → 결과 흐름, 역할별 화면, 입력 라우팅, 스냅숏 이벤트 → 연출
```

* **PvP 실행**: `node server.js` (포트 변경 `PORT=9000 node server.js`) → 출력된 `http://주소:8080` 으로
  두 기기가 접속해 PvP 를 누른다. 먼저 접속한 사람이 1회 초 공격이다. Windows 방화벽이 Node 를 막으면 폰이 못 붙는다.
* **연습 모드**: 서버 없이 `index.html` 을 직접 열어도 된다 (`file://` 에서는 PvP 버튼만 막힌다).
* three.js 는 CDN(UMD, 전역 `THREE`)에서 로드하므로 모든 기기에 인터넷 연결이 필요하다.
* **`?debug`**: 공격자 쪽 관객 난입이 매 구간 나오고, `F` 키로 피버가 가득 찬다.
* **빌드/린트/테스트 명령 없음.** 문법 확인은 `node --check js/*.js server.js`.
* 규칙 검증은 `vm.runInThisContext` 로 `data → match → attack → defense → bot → net` 순서로 로드하면
  DOM 없이 돈다 (최상위 `var` 가 전역이 되어 브라우저와 같다). `setTimeout` 을 가짜 시계로 바꾸면
  봇 대 봇 1이닝을 몇 초 만에 돌릴 수 있다.

모든 스크립트는 classic script 다. `type="module"` 로 바꾸면 `file://` 연습 모드가 깨진다.
`module.exports` 가드는 서버가 `require` 하는 `data.js` · `match.js` 에만 있다.

**설계 근거 문서**: 현재 게임은 `새아이디어.md`(PvP 명세)가 기준이고, 공통 원칙은 `기획안.md` 의 § 번호로
참조한다. `docs/` 요약들은 싱글 버전 기준이다. **코드와 문서가 충돌하면 코드가 우선이다.**

### 문서 자체의 불일치 — 이미 결론이 났다

* 기획안 §9 의 "좌상 입력 = 오른쪽 상체 회피" 는 §11 정답 표와 어긋난다. **§11 표가 우선**이고
  액션 명칭은 "입력한 방향으로 몸을 피한다" 로 정합화했다 (`ACTION_NAME`). 되돌리지 않는다.
* 새아이디어 C03 의 "대각선 반대 **상단** 칸" 은 문구가 모순이다(대각선 반대는 아래 칸).
  **반대편 위 칸**(좌상↔우상)으로 확정했다. C04 는 반대편 아래 칸. 둘 다 `MIRROR_ZONE` 을 쓴다.

## 이 게임이 무엇인가

한 줄: **타격으로 CHANCE 를 얻고, 주루 회피로 세이프를 지켜내며, 3B 에서 피버로 홈런을 노리는 1v1 대전.**

```
[수비] 2단계 투구 → [공격] 타격 판정 → CHANCE → 주루 (수비는 카드로 기습) → 세이프 / 아웃 / 피버 HR
```

타격 시점에 결과를 확정하지 않는다는 원칙(§3, §43)은 그대로다. 단 **CHANCE 는 주루 중에 오르내리지 않는다**
(사용자 결정). 주루는 **세이프냐 아웃이냐**를 가르고, **3B CHANCE 에서만 피버로 HR** 이 된다.

### 구조 — 판정은 공격자 한 곳에서만

* **AttackSim 이 모든 판정을 한다.** 공격자 클라이언트(연습 수비에서는 봇 타자)에만 있다.
* 공격자는 `RUN_STATE` 스냅숏을 초당 20회 보내고, **수비자는 그 스냅숏으로 같은 장면을 그린다**
  (`DefenseSide.liveView` 가 받은 뒤 흐른 시간만큼 보간).
* 서버·LocalLink 는 `MatchCore` 로 **중계·아웃·점수·이닝만** 관리한다.
* **연출은 `snapshot().events` 에서 나온다** (`game.js` 의 `playEvent`). 공격자·수비자 화면이 같은 길을 쓴다.
  새 연출이 필요하면 AttackSim 에서 이벤트를 push 하고 `playEvent` 에서 받는다.

한 타석 상태 (AttackSim):

```
WAIT_PITCH → PITCH → BAT_RESULT → CHANCE → RUNNING → OUTRO → DONE
                         ↓ MISS              ↓ 3B + 피버
                        DONE               HR_RUN → DONE
```

매치 (MatchCore): `MATCH_START → AT_BAT_START → … 3아웃 → INNING_CHANGE(역할 교대) → … 3아웃 → GAME_OVER`.
타석 결과 후 2.5초 뒤 자동으로 다음 타석. 연습 모드는 반 이닝(3아웃)만.

### 주루 구간

* 한 구간 = 베이스 하나. **구간 길이는 시간으로 고정**: 홈→1루 5.0초 / 1→2루 5.5초 / 2→3루 8.0초
  (`LEG_DURATION`). JUST/PERFECT 타격은 앞 두 구간이 1.5배 빠르다 (`FAST_RUN`).
* 구간 사이에 멈추지 않는다. 베이스를 밟는 순간 날아오던 장애물은 **다음 구간으로 이어진다**.
  마지막 베이스에 닿으면 남은 장애물은 버린다.
* **레벨은 길이가 아니라 밀도(속도·간격)만 올린다.** 레벨은 타자별 타석 수로 오른다 (`levelOf`).
* 최종 결과는 실제로 밟은 베이스 수(`sim.base`)다.

## 반드시 지켜야 하는 규칙

수치는 튜닝해도 되지만 아래 원칙은 바꾸지 않는다.

- **주루에 자유 이동이 없다.** 자동 전진, 조작은 회피뿐. 3레인 이동으로 되돌리지 않는다 (§7-2).
- **입력은 화면 4분할 터치.** 타격·회피·투구 모두 같은 네 칸이다. 예외는 피버 버튼(상단 가운데)과
  수비 손패 카드뿐이다 (§8, §9).
- **상체와 하체의 정답 규칙이 서로 반대다.** 의도적인 혼란 요소다. 되돌리지 않는다:

  | 위험 위치 | 위험 유형 | 정답 입력 | 규칙 |
  | -- | -- | -- | -- |
  | 좌상 / 우상 | 공 · 난입 관객 | 우상 / 좌상 | **반대쪽** 으로 몸을 피한다 |
  | 좌하 / 우하 | 수비수 태클 | 좌하 / 우하 | **같은 쪽** 다리를 들어 넘긴다 |

  구현체는 `js/data.js` 의 `DANGER_MAP` 하나뿐이다. 필드·카드·관객 장애물 모두 이 표를 쓴다.

- **장애물과 카드는 데이터다** (§40). `OBSTACLES`(Type/Spawn/RequiredInput/MoveSpeed/ReactionTime/
  Reward/Penalty + costGain), `CARDS`(slots/spawns/switchAt/effect). 카드별 if/else 를 만들지 않는다.
  교란 효과는 `effect.type` 두 가지(`decel`, `dust`)뿐이다.
- **장애물 접근은 거리값 하나로 계산한다** (§25). 충돌 지점은 `distance 0 = z 0`(플레이어 몸 중심).
- **필드 장애물은 완전 랜덤 금지.** 패턴 풀 + 가중 랜덤 + 구간 예산(1·2·3점) (§14, §32).
  필드끼리는 유효 창이 겹치지 않게 간격을 보정하고(§13-3), 구간마다 최소 1개는 남긴다.
  **장애물 겹침은 수비 카드로만 생긴다** — 사용자 결정으로 보정하지 않는다(동시에 오면 두 칸을 같이 누른다).
- **실패 이유가 눈에 보여야 한다** (§37). WRONG 은 잘못 누른 쪽으로 피한 뒤 맞고 정답을 글로 보여준다.
- **실패 연출은 짧게.** OUT 후 0.75초 뒤 결과 화면 (§36).

## 사용자가 결정한 것 — 다시 묻지 않는다

### 회피 판정 = 누르는 즉시 판정 + 유효 창

누른 칸마다 따로 판정한다 (`AttackSim.dodge`).
1. 충돌까지 `reactionTime`(0.45초) 안으로 들어온 장애물 중 그 칸이 정답인 것 → 가장 가까운 것 성공.
   `RUN.PERFECT_WINDOW`(0.20초) 이내면 PERFECT, 그 전이면 NICE.
2. 유효 창 안에 장애물은 있는데 정답이 아니면 → 가장 가까운 장애물 WRONG.
3. 유효 창 안에 아무것도 없으면 → **헛회피: 0.35초 경직**(게이지·연속 기록은 그대로).
4. 끝까지 안 눌리면 TOO LATE.

**충돌 직전이 최고 판정이다. 일찍 누를수록 좋은 구조로 되돌리지 않는다** (눈에 보이는 충돌과 판정이 어긋난다).
C03·C04 전환(충돌 0.55초 전)과 C07 감속(0.5초 전)은 유효 창이 열리기 직전에 일어나 "미리 누른 손" 을 헛회피로 만든다.

### 타격 = 타이밍 + 코스, 타이밍 링 없음

수비자가 두 칸을 고른다 (같은 칸 = 직구, 다른 칸 = 시작 칸에서 도착 칸으로 휘는 변화구, 3초 넘기면 무작위 직구).
**쳐야 할 칸은 도착 칸**이다.

| 타이밍 | 누른 칸 | 결과 |
| -- | -- | -- |
| ≤ 0.230s | 도착 칸 | JUST(≤0.030) 3B / PERFECT(≤0.070) 3B / GREAT(≤0.140) 2B / GOOD 1B |
| ≤ 0.230s | 옆 칸 | 빗맞은 1B. 그중 **12% 는 LUCKY** — 1B/2B 반반, 수비 코스트 -2, 주루 시작 후 2초 카드 잠금 |
| ≤ 0.230s | 대각선 반대 | MISS → 아웃 |
| > 0.230s / 무스윙 | — | MISS → 아웃 |

파울은 폐지했다. JUST 는 게이지 60 에서 출발, JUST·PERFECT 는 앞 두 구간 가속.

### 주루 게이지 · 아웃 · 피버

* **런 게이지 = 체력.** 시작 50(JUST 60), 최대 100. PERFECT +25 / NICE +15 × 연속 배율(최대 1.5),
  WRONG -35 / TOO LATE -45. **0 이하면 즉시 아웃.** CHANCE 상승·하락과 "3B 이후 절반" 규칙은 없다.
* **아웃 4종**: 타격 MISS / 게이지 0 이하 / 한 구간 TOO LATE 2회 / 한 구간 성공 0개(피버 무적을 쓴 구간은 예외).
  "구간 성공 0개" 는 공짜로 베이스를 밟는 버그를 막으려고 유지한다.
* **피버**: 타석마다 0 에서 시작. PERFECT +10 / NICE +5 × 연속 배율. 가득 차면 상단 가운데 버튼(PC SPACE).
  CHANCE 3B 면 **HR 직행**(무적 질주 2.6초), 아니면 **무적 2.5초 + 게이지 +30 + 1.5배 돌진**.
  수치 근거: 봇 대 봇 40경기에서 20/12 는 세이프의 40% 가 HR 이었고, 10/5 는 약 9% 였다.
* **관객 난입**: 구간마다 7%, 위 좌/우에서 뛰어든다(정답은 공과 같은 표). 성공 시 피버 +50, 게이지 +40.
  필드 장애물 충돌과 0.6초 이상 떨어뜨린다. 수비 코스트는 충전하지 않는다.
* **바람**: 필드·카드 장애물마다 속도 ±5~10%.

### 수비 · 카드

* 코스트는 반 이닝 시작 3, 최대 10. **시간으로 차지 않고 타자의 회피 성공마다 +1.**
* 8장 덱(C01~C08 한 장씩) 중 손패 4장. **카드 위치는 손패에 들어올 때 무작위로 정해져 카드에 표시**되고,
  누르면 즉시 발동한다. 쓴 카드는 덱으로 돌아가고, 보충은 손패에 있는 카드와 방금 쓴 카드를 뺀 나머지에서 뽑는다.
* C07 은 **다가오는 가장 가까운 공**(감속 지점 전)을 감속시킨다. 대상이 없으면 쓸 수 없다.
* 마지막 베이스 전에 부딪힐 수 없는 시점에는 장애물 카드를 쓸 수 없다 (`DefenseState.blockReason`).
* 수비자 화면은 공격자와 같은 3인칭 화면 + 하단 손패. 흙먼지(C08)는 공격자 화면에만 보이고,
  **카드 발동은 공격자에게 알리지 않는다**(기습).

### 매치 · 모드

* **득점 = 루타 점수 합산** (1B 100 / 2B 200 / 3B 350 / HR 600). 주자를 베이스에 남기지 않는다.
* **동점 = 무승부.** 1회 말은 끝내기 없이 3아웃까지.
* 방은 하나, 선착 2명. 한쪽이 나가면 남은 쪽에 `OPPONENT_LEFT` 후 로비로.
* **연습 모드 = 역할 선택** (봇 수비 상대로 공격 / 봇 타자 상대로 수비). 봇은 확률로만 움직인다.
* 3D 는 three.js 프리미티브만 (§26). 회피는 기울임·다리 들기 트윈으로만 표현한다.

### 패킷

`{ t, p }` JSON. 새아이디어 5장의 9종을 쓰고 아래를 보강했다.
`CARD_PLAY` 에 `zones`, `INNING_CHANGE` 에 받는 사람의 `role`, `GAME_OVER` 에 `scores`(무승부 `winner: null`).
추가: `AT_BAT_START`, `RUN_STATE`(스냅숏), `SAFE`, `WAITING`, `ROOM_FULL`, `OPPONENT_LEFT`.
타석 결과 패킷은 `seq` 로 이전 타석의 늦은 패킷을 버린다.

여전히 확정되지 않은 것: 없음. 새 항목이 생기면 여기에 적는다.

## 만들지 않는 것 (§27, §44)

핵심 재미가 검증되기 전에 만들면 프로젝트가 비대해진다. 요청받지 않은 한 손대지 않는다.

실제 경기 규칙 · 볼/스트라이크 · 정교한 투수·수비 AI(연습 봇은 확률 봇까지만) · 베이스 이동 AI · 다수 주자 ·
수비 포지셔닝 · 야구 스탯 · 선수 성장 · 커스터마이징 · 스킨 · 장비 · 상점 · 재화 · 뽑기 · 강화 · 랭킹 ·
인터넷 매칭·계정·여러 방·재접속 복구(LAN 방 하나까지만) · 덱 편집 · 스테이지 선택 · 실제 야구장 제작 · 복잡한 메타 시스템.

§35 의 주루 중 STOP/RUN 선택은 첫 프로토타입에 넣지 않는다. §34 의 장애물/액션 확장도 이후 과제다.

## 무엇을 먼저 검증하는가 (§29, §30)

**타격보다 주루 시스템 검증이 우선이다** (§46). 가장 중요한 질문은 "4방향 대응 주루가 실제로 재미있는가?" 다.

우선순위: ① 공 좌상/우상 → 반대쪽 상체 회피 ② 수비수 좌하/우하 → 같은 쪽 다리 회피
③ 수비 카드 기습이 "읽고 반응하는" 심리전으로 느껴지는가.

성공 기준은 "작동한다" 가 아니다 (§30 Q1–Q6). CHANCE 상승을 뺐으므로 §30 Q5 의 "2B 가 3B 가 될 것 같은"
순간은 현재 구조에 없다. 지금 구조에서 욕심이 생기는 지점은 **3B CHANCE 에서 피버를 모아 HR 을 노리는 순간**이다.
