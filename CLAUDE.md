# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 현재 상태

**코드가 아직 없다.** 이 저장소에는 `기획안.md` 하나뿐이다.

따라서 빌드 / 실행 / 린트 / 테스트 명령이 존재하지 않는다. 프로젝트 파일, 패키지 매니저,
테스트 러너 전부 미생성 상태다. 명령어를 추측해서 실행하지 말고, 실제 프로젝트가 생성되면
이 섹션을 가장 먼저 갱신한다.

**기술 스택도 아직 미결정이다.** 기획안 §1-3 은 "Unity 2D/2.5D 또는 간단한 3D 구현 가능"으로
열어둔 상태이고, §38 은 Unity 스타일 Manager 구조를 예시로 들지만 이는 제안일 뿐 확정이 아니다.
스캐폴딩을 요구받으면 **어떤 스택으로 만들 것인지 먼저 사용자에게 확인한다.** 임의로 정하지 않는다.

`기획안.md` 는 모든 설계 판단의 근거다. 작업 전에 관련 섹션을 읽고, 근거를 밝힐 때 **§ 번호로
참조**한다. 코드가 생긴 뒤에는 코드와 문서가 충돌하면 코드가 우선이다.

## 이 게임이 무엇인가 (§2, §43, §46)

한 줄: **타격으로 루타의 가능성을 만들고, 주루 액션으로 그 가능성을 실현한다.**

핵심 차별점은 **타격 결과가 곧 최종 결과가 아니라는 것**이다 (§3).

```
타격 판정 → CHANCE(잠재 결과) 생성 → 주루 회피 → 최종 루타 확정
```

예: `2B CHANCE` + 주루 PERFECT → `3B`.  `2B CHANCE` + 주루 실패 → `1B`.

이 4단계 연결이 프로토타입의 존재 이유다. 어떤 이유로도 이 사슬을 끊거나,
타격 시점에 결과를 확정하는 구조로 만들지 않는다 (§43).

상태 머신 (§39):

```
TITLE → PITCH → BAT → BAT_RESULT → CHANCE → RUNNING → RESULT → PITCH
                                              ↓
                                             OUT → RESULT
```

한 사이클은 12~20초를 넘기지 않는다 (§4-2: 타격 3~5초 / 주루 7~12초 / 연출 2초).

## 반드시 지켜야 하는 규칙

수치는 튜닝해도 되지만 아래 원칙은 바꾸지 않는다.

- **주루에 자유 이동이 없다.** 캐릭터는 자동 전진하고, 플레이어가 조작하는 것은 **회피 행동뿐**이다.
  3레인 좌우 이동 방식으로 되돌리지 않는다 (§7-2).
- **입력은 화면 4분할 터치.** 좌상/우상/좌하/우하 영역 터치가 각각 상체 회피 2종·다리 회피 2종에
  대응한다. 큰 버튼 그래픽은 필수가 아니다 (§8, §9).
- **정답 입력은 위험의 반대쪽으로 고정한다** (§11). 이 매핑은 프로토타입 학습 곡선을 위한 의도적
  단순화이므로 임의로 바꾸지 않는다:

  | 위험 위치 | 위험 유형 | 정답 입력 |
  | -- | -- | -- |
  | 좌상 | 날아오는 공 | 우상 |
  | 우상 | 날아오는 공 | 좌상 |
  | 좌하 | 수비수 태클 | 우하 |
  | 우하 | 수비수 태클 | 좌하 |

- **장애물은 데이터다** (§40). `Type / SpawnPosition / RequiredInput / MoveSpeed / ReactionTime /
  Reward / Penalty` 를 가진 데이터로 정의하고, 장애물 종류별 if/else 분기를 만들지 않는다.
- **장애물 접근은 거리값 하나로 계산한다** (§25). 실제 3차원 좌표 계산 대신 `Distance: 100 → 0`
  방식으로 접근시킨다. 3D든 가짜 3D든 원리는 동일하며, 프로토타입에서는 복잡한 3D 애니메이션을
  만들지 않는다 (§26).
- **완전 랜덤 생성 금지.** 패턴 풀 + 가중 랜덤을 쓰고 (§14), 한 주루 구간에 **난이도 예산**을
  배정해 패턴을 조합한다 (§32: 쉬움 1점 / 보통 2점 / 어려움 3점, 구간 총합 고정).
- **"나쁜 랜덤"은 버그로 취급한다** (§13-3). 화면 밖 갑작스러운 등장, 지나치게 짧은 대응 시간,
  시야 밖 충돌, 입력으로 해결 불가능한 상황 — 전부 금지. 운은 *상황을 결정*하고
  실력이 *상황을 해결*한다 (§13-1). 목표 비율은 실력 70 / 운 30 (§31).
- **실패 이유가 눈에 보여야 한다** (§37). "왜 맞았지?"가 나오면 조작 체계 자체의 실패다.
- **실패 연출은 짧게.** OUT 후 긴 연출 없이 즉시 결과 화면으로 간다 (§36).

## 아직 정해지지 않은 것 — 임의로 결정하지 말고 물어본다

기획안 자체가 두 안을 병기하거나 판단을 보류한 지점들이다. 구현하다 마주치면 확인한다.

- **주루 결과 계산 방식.** §17 은 단순 성공 횟수 표(6/6 → +2단계 …)를, §18 은 연속 성공 게이지를
  제시하고 후자를 추천한다. 둘은 체감이 크게 다르다.
- **HR 포함 여부.** §5-3 은 HR을 일반 타격 결과가 아닌 상위 특별 결과로 두고, §6-3 은 초기
  프로토타입에서는 **1B/2B/3B까지만 검증**할 것을 권한다. 기본값은 HR 제외다.
- **3D 사용 범위.** §26 은 첫 프로토타입에서 3D 애니메이션을 만들지 않는다고만 정했고,
  실제 렌더링 방식은 열려 있다.

## 만들지 않는 것 (§27, §44)

핵심 재미가 검증되기 전에 만들면 프로젝트가 비대해진다. 요청받지 않은 한 손대지 않는다.

실제 경기 규칙 · 볼/스트라이크 · 투수 AI · 정교한 수비 AI · 베이스 이동 AI · 다수 주자 ·
수비 포지셔닝 · 야구 스탯 · 선수 성장 · 커스터마이징 · 스킨 · 장비 · 상점 · 재화 · 뽑기 ·
강화 · 랭킹 · 온라인 · 스테이지 선택 · 실제 야구장 제작 · 복잡한 메타 시스템.

§35 의 주루 중 STOP/RUN 선택(더 뛸 것인가)은 매력적이지만 **첫 프로토타입에는 넣지 않는다**고
명시되어 있다. §34 의 장애물/액션 확장도 마찬가지로 이후 과제다.

## 무엇을 먼저 검증하는가 (§29, §30)

**타격보다 주루 시스템 검증이 우선이다** (§46). 가장 중요한 질문은
"4방향 대응 주루가 실제로 재미있는가?"이므로, 처음부터 야구 게임처럼 만들면 안 된다.

우선순위: ① 공 좌상/우상 → 반대쪽 상체 회피 ② 수비수 좌하/우하 → 반대쪽 다리 회피
③ 타격 결과가 1B/2B/3B를 결정하는 구조.

MVP 필수 목록은 §27, 난이도 단계는 §15, 스테이지 예시는 §33 에 있다.

성공 기준은 "작동한다"가 아니다 (§30 Q1–Q6). 특히 **Q5 — "2B CHANCE가 3B가 될 것 같을 때
욕심이 생기는가"** 가 핵심 리텐션 지표다. 기능 구현 후 이 감정이 발생하는지를 기준으로 판단한다.
