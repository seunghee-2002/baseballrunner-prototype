# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 현재 상태

**LAN 1v1 PvP 웹 프로토타입 v2 가 구현되어 있다** (`docs/기획서-v2.md` 기준). 빌드 도구·패키지 매니저·테스트 러너는 쓰지 않는다.
이전 v1(CHANCE·런 게이지·피버 규칙, `docs/기획서.md` 기준)은 `v1/` 에 수정 없이 보존되어 있다
(`v1/index.html` 을 열거나 `node v1/server.js` 로 그대로 돈다).

기획서 v2 는 Unity 3D + P2P 호스트를 전제로 쓰였지만, 이 프로토타입은 **웹으로 유지**한다 (사용자 결정).
웹에서는 `server.js` 가 호스트 역할(아웃·점수·이닝·핸디캡)을 하고, 판정은 기획서대로 공격자 기기에서 한다.

```
index.html      화면 골격 (세로 9:16), 4분할 입력, HUD(점수·카운트·하트·남은 거리), 수비 손패, 오버레이
style.css       HUD / 손패 / 연출 / 오버레이 / 덱 편집 스타일
server.js       LAN 서버. Node 내장 모듈만. 정적 파일 + WebSocket(/ws) + 방 하나(선착 2명)
js/data.js      DANGER_MAP · 장애물 · 패턴 풀 · 구간 4개(LEGS) · 타격 등급 · 투구 카드 · 장애물 카드 · 매치 상수   (서버 공용)
js/match.js     MatchCore — 3이닝·공격 횟수·2아웃 조기 종료·핸디캡·점수·승패. 중계 규칙              (서버 공용)
js/net.js       NetLink(WebSocket) / LocalLink(연습 모드: 페이지 안에서 MatchCore 구동)
js/attack.js    AttackSim — 투구·카운트·타격·생명제 주루·회피 판정·교란 경고 (DOM 없음) / AttackSide — 통신 껍데기
js/defense.js   DefenseState — 투구 카드·장애물 손패·코스트·쿨다운·사용 가능 판정 / DefenseSide — 통신 껍데기
js/bot.js       연습용 확률 봇 (BotDefender / BotBatter)
js/scene.js     three.js 렌더링 (프리미티브만). 뷰 4개(bat / pitch / run / field). 스냅숏 distance -> 3D 좌표
js/ui.js        HUD · 손패 · 덱 편집 · 오버레이 · 오실레이터 효과음
js/game.js      로비 → 매치 → 결과 흐름, 역할별 뷰, 입력 라우팅, 스냅숏 이벤트 → 연출
```

* **PvP 실행**: `node server.js` (포트 변경 `PORT=9000 node server.js`) → 출력된 `http://주소:8080` 으로
  두 기기가 접속해 PvP 를 누른다. 먼저 접속한 사람이 1회 초 공격이다. Windows 방화벽이 Node 를 막으면 폰이 못 붙는다.
* **연습 모드**: 서버 없이 `index.html` 을 직접 열어도 된다 (`file://` 에서는 PvP 버튼만 막힌다). 반 이닝(공격 3회)만 한다.
* three.js 는 CDN(UMD, 전역 `THREE`)에서 로드하므로 모든 기기에 인터넷 연결이 필요하다.
* **`?debug`**: 공격자 쪽 관객 난입이 매 구간 나온다.
* **빌드/린트/테스트 명령 없음.** 문법 확인은 `node --check js/*.js server.js`.
* 규칙 검증은 `vm.runInThisContext` 로 `data → match → attack → defense → bot → net` 순서로 로드하면
  DOM 없이 돈다 (최상위 `var` 가 전역이 되어 브라우저와 같다). `setTimeout` 을 가짜 시계로 바꾸면
  봇 대 봇 3이닝을 1초 안쪽에 돌릴 수 있다.

모든 스크립트는 classic script 다. `type="module"` 로 바꾸면 `file://` 연습 모드가 깨진다.
`module.exports` 가드는 서버가 `require` 하는 `data.js` · `match.js` 에만 있다.

**설계 근거 문서**: `docs/기획서-v2.md` 가 기준이다. `docs/기획서-v2-변경이력.md` 는 v1 대비 변경 논의 기록,
`docs/기획서.md` 는 v1 코드 기준 문서다. **코드와 문서가 충돌하면 코드가 우선이다.**
[미정]/[가안] 은 문서 부록 C 에 있다. 사용자가 확정한 것만 확정으로 바꾼다.

### 문서 자체의 불일치 — 이미 결론이 났다

* 좌상 입력 = "입력한 방향으로 몸을 피한다" 로 정합화했다 (`ACTION_NAME`). 되돌리지 않는다.
* C03 은 **반대편 위 칸**(좌상↔우상), C04 는 반대편 아래 칸. 둘 다 `MIRROR_ZONE` 을 쓴다.

## 이 게임이 무엇인가

한 줄: **타격으로 생명을 얻고, 주루 회피로 생명을 지키며, 생명이 남아 있는 한 홈까지 달리는 1v1 대전.**

```
[수비] 투구 카드 + 시작 칸 → [공격] 타격 → 생명 → 주루 (수비는 장애물 카드로 기습) → 생명 0 = 마지막 베이스 / 홈 = 홈런
```

목표 루타(CHANCE)·런 게이지·피버는 **없다**. 홈런은 특별한 장치가 아니라 끝까지 안 맞는 것이다.

### 구조 — 판정은 공격자 한 곳에서만

* **AttackSim 이 모든 판정을 한다.** 공격자 클라이언트(연습 수비에서는 봇 타자)에만 있다.
* 공격자는 `RUN_STATE` 스냅숏을 초당 20회 보내고, **수비자는 그 스냅숏을 자기 카메라로 그린다**
  (`DefenseSide.liveView` 가 받은 뒤 흐른 시간만큼 보간).
* 서버·LocalLink 는 `MatchCore` 로 **중계·아웃·점수·이닝·핸디캡만** 관리한다.
* **연출은 `snapshot().events` 에서 나온다** (`game.js` 의 `playEvent`). 공격자·수비자 화면이 같은 길을 쓴다.
  새 연출이 필요하면 AttackSim 에서 이벤트를 push 하고 `playEvent` 에서 받는다.

한 타석 상태 (AttackSim):

```
WAIT_PITCH → PITCH → BAT_RESULT ─┬─ 스트라이크·볼 (카운트 남음) → WAIT_PITCH
                                 ├─ 2스트라이크 → DONE (삼진)
                                 ├─ 2볼 → RUNNING (2루에서, 생명 2)
                                 └─ 안타 → RUNNING (홈에서)
RUNNING → 홈 도착 DONE (HR) / 생명 0 → OUTRO(0.75초) → DONE
```

매치 (MatchCore): `MATCH_START → 반 이닝 [AT_BAT_START … 공격 3회 소진 또는 2아웃] → INNING_CHANGE(역할 교대, 이닝 끝이면 핸디캡 판정) → … 3회 말 → GAME_OVER`.
타석 결과 후 1.5초 뒤 자동으로 다음 타석. 연습 모드는 반 이닝만.

### 주루 구간

* 한 구간 = 베이스 하나, **4구간**(홈→1루→2루→3루→홈). **구간 길이는 시간으로 고정**: 4.0 / 4.5 / 5.5 / 6.0초 (`LEGS`).
* **보너스 속도**: PERFECT 는 3루까지, GREAT 는 2루까지 구간 시간이 1.5배 빨리 흐른다 (`boostLegs`). 장애물 밀도는 그대로라 그 구간 장애물 수가 준다.
* 구간 사이에 멈추지 않는다. 베이스를 밟는 순간 날아오던 장애물은 **다음 구간으로 이어진다**. 홈에 닿으면 남은 장애물은 버린다.
* **난이도는 구간 순서로만 오른다** (예산 1/2/3/3, 속도·간격·어려운 패턴 가중치). 타석 수 레벨은 없다.
* 최종 결과는 실제로 밟은 베이스 수(`sim.base`)다.

## 반드시 지켜야 하는 규칙

수치는 튜닝해도 되지만 아래 원칙은 바꾸지 않는다.

- **주루에 자유 이동이 없다.** 자동 전진, 조작은 회피뿐. 3레인 이동으로 되돌리지 않는다.
- **입력은 화면 4분할 터치.** 타격·회피·투구 시작 칸 모두 같은 네 칸이다. 예외는 수비 손패 카드뿐이다. 피버 버튼은 없다.
- **상체와 하체의 정답 규칙이 서로 반대다.** 의도적인 혼란 요소다. 되돌리지 않는다:

  | 위험 위치 | 위험 유형 | 정답 입력 | 규칙 |
  | -- | -- | -- | -- |
  | 좌상 / 우상 | 공 · 난입 관객 | 우상 / 좌상 | **반대쪽** 으로 몸을 피한다 |
  | 좌하 / 우하 | 수비수 태클 | 좌하 / 우하 | **같은 쪽** 다리를 들어 넘긴다 |

  구현체는 `js/data.js` 의 `DANGER_MAP` 하나뿐이다. 필드·카드·관객 장애물 모두 이 표를 쓴다.

- **장애물과 카드는 데이터다.** `OBSTACLES`, `CARDS`(slots/spawns/switchAt/effect), `PITCH_CARDS`(move/timeMul/cost).
  카드별 if/else 를 만들지 않는다. 교란 효과는 `effect.type` 두 가지(`decel`, `dust`)뿐이다.
- **장애물 접근은 거리값 하나로 계산한다.** 충돌 지점은 `distance 0 = z 0`(플레이어 몸 중심).
- **필드 장애물은 완전 랜덤 금지.** 패턴 풀 + 가중 랜덤 + 구간 예산. 필드끼리는 유효 창이 겹치지 않게 간격을 보정하고,
  구간마다 최소 1개는 남긴다. **카드 장애물의 겹침은 보정하지 않는다** (동시에 오면 두 칸을 같이 누른다).
- **실패 이유가 눈에 보여야 한다.** 회피 BAD 는 잘못 누른 쪽으로 피한 뒤 맞고 정답을 글로 보여준다. 스트라이크도 이유를 글로 보여준다.
- **실패 연출은 짧게.** 아웃·생명 소진 후 0.75초 뒤 결과.

## 사용자가 결정한 것 — 다시 묻지 않는다

### 등급 체계는 하나 — PERFECT / GREAT / GOOD / BAD

타격도 회피도 이 네 등급이다. JUST·NICE·WRONG·TOO LATE 는 폐기했다.

### 투구 = 투구 카드 + 시작 칸

* 수비자가 투구 손패(6장 풀 중 3장, 이닝마다 재섞기) 또는 **직구 고정 버튼(코스트 0)** 을 고른 뒤 칸을 탭한다.
  **선택은 두 단계를 차례로 한다 (사용자 결정)**: 구질 단계 3초(넘기면 직구) → 칸 단계 3초(구질을 고른 순간 새로 셈, 넘기면 고른 구질로 무작위 칸).
  칸 단계에서는 구질을 바꿀 수 없다. 시간은 `PITCH.PICK_TIME` / `PITCH.ZONE_TIME`.
* 공은 시작 칸에서 카드의 이동 방향대로 **도착 칸**에 온다. 네 칸 밖으로 나가면 **볼**.
* 투구 카드도 코스트를 쓴다 (장애물 카드와 같은 코스트 풀). 구속은 카드마다 기본값 × 투구마다 ±8%.
* **P5 대각 커브는 항상 대각선 반대 칸으로 휘고 볼이 되지 않는다** (사용자 결정).
* **수비자는 투수 뒤 시점**이라 좌우가 타자 시점과 반대다. 수비자 탭 칸과 카드 이름("좌→우")은 수비자 화면 기준이고,
  타자 시점으로 바꾸는 곳은 `DefenseState.tapPitch`(`MIRROR_ZONE`)와 `pitchPath`(dc 반전) 두 줄뿐이다.

### 타격 = 타이밍 + 코스, 카운트

| 타이밍 | 누른 칸 | 결과 |
| -- | -- | -- |
| ≤ 0.230s | 도착 칸 | PERFECT(≤0.040) 생명 3·3루까지 가속 / GREAT(≤0.100) 생명 2·2루까지 가속 / GOOD(≤0.160) 생명 2 / BAD 생명 1 |
| ≤ 0.230s | 옆 칸 | **파울 = 스트라이크**. 그중 **25% LUCKY** — 생명 1 로 주루 |
| ≤ 0.230s | 대각선 반대 | 스트라이크 |
| > 0.230s / 무스윙(도착 후 0.24초) | — | 스트라이크 |
| 볼에 스윙 | — | 스트라이크 |

* **2스트라이크 = 삼진 아웃** (파울로도). **2볼 = 2루에서 생명 2 로 주루 시작** (최소 2루).
* LUCKY 수비 페널티(코스트 차감·카드 잠금)는 **없다** (사용자 결정).

### 주루 = 생명제

* 생명은 타격이 정한 수로 시작, 최대 3. **회피 BAD = 생명 -1**, 회피 성공은 회복하지 않는다.
* **생명 0 = 마지막으로 밟은 베이스에서 세이프.** 1루 전이면 아웃.
* 아웃은 **삼진 / 1루 전 생명 0** 두 가지뿐이다.
* 관객 난입: 구간마다 7%, 보상 없음, 수비 코스트 없음. 바람: 장애물마다 속도 ±5~10%.

### 회피 판정 = 누르는 즉시 판정 + 유효 창

누른 칸마다 따로 판정한다 (`AttackSim.dodge`).
1. 충돌까지 `reactionTime`(0.45초) 안으로 들어온 장애물 중 그 칸이 정답인 것 → 가장 가까운 것 성공.
   0.15초 이내 PERFECT, 0.30초 이내 GREAT, 그 전 GOOD. 차이는 연출·기록뿐이고 연속 배율은 없다.
2. 유효 창 안에 장애물은 있는데 정답이 아니면 → 가장 가까운 장애물 BAD(틀린 칸).
3. 유효 창 안에 아무것도 없으면 → **헛회피: 0.35초 경직**.
4. 끝까지 안 눌리면 BAD(반응 없음).

**충돌 직전이 최고 판정이다. 일찍 누를수록 좋은 구조로 되돌리지 않는다.**
C03·C04 전환(충돌 0.55초 전)과 C07 감속(0.5초 전)은 유효 창이 열리기 직전에 일어나 "미리 누른 손" 을 헛회피로 만든다.

### 수비 · 코스트 · 카드

* 코스트는 **타석마다 4 지급**, 남은 코스트 이월, 최대 10. 타자의 필드·카드 장애물 회피 성공마다 +1. 시간으로 차지 않는다.
  공수 교대 때 코스트·손패 초기화(0 에서 시작, 첫 타석 지급이 시작 코스트).
* 장애물 카드 8종 중 **플레이어가 매치 전에 고른 6장**이 덱(로비 "덱 편집", 브라우저 localStorage). 손패 3장.
  카드 위치는 손패에 들어올 때 무작위로 정해져 카드에 표시. 보충은 손패와 방금 쓴 카드를 뺀 나머지에서. 연습 봇 덱은 무작위 6장.
* 카드 코스트 C01~C08 = 1 / 1 / 3 / 3 / 5 / 5 / 3 / 3. 발동 후 **전역 쿨다운 0.5초**.
* **기습은 읽을 수 있어야 한다**: 카드 장애물은 필드 장애물과 **색이 다르다**(양쪽 화면).
  교란 카드(C07·C08)는 공격자 화면에 **경고 0.5초 뒤 적용**(감속 = 대상 공에 링 / 흙먼지 = 가장자리 먼지).
  어떤 카드를 언제 썼는지는 공격자에게 알리지 않는다. "○○ 발동" 문구는 수비자 화면에만.
* C07 은 경고 시간을 더해도 감속 지점 전인 가장 가까운 공이 있어야 쓸 수 있다. 장애물 카드는 홈 전에 부딪힐 수 없으면 못 쓴다 (`DefenseState.blockReason`).

### 화면 · 뷰

* 공격자와 수비자는 **다른 뷰**를 본다 (`Scene3D.setView`).
  공격자: 타격 `bat`(스트라이크존 높이, 위 칸·아래 칸 색 구분) / 주루 `run`(주자 뒤 3인칭).
  수비자: 투구 `pitch`(투수 뒤, 좁은 화각) / 주루 `field`(필드 전경 + 하단 손패).
* **수비자는 필드 장애물 생성을 약 1초 먼저 본다** (스냅숏 `preview` → 반투명 예고). 구간 경계를 넘는 예고를 위해 AttackSim 이 다음 구간 일정을 미리 만든다.
* 주루 HUD 는 **오른쪽 하트 + 위쪽 다음 베이스까지 거리 바**만 또렷하고, 점수판은 흐리게 한다.
* 3D 는 three.js 프리미티브만. 회피는 기울임·다리 들기 트윈으로만 표현한다.

### 매치 · 모드

* **3이닝**, 반 이닝 = **공격 3회**(타석 수), **2아웃이면 조기 종료**. 볼·파울로 다시 던지는 것은 같은 타석이다.
* **득점 = 밟은 마지막 베이스 점수** (1루 50 / 2루 150 / 3루 400 / 홈런 1000). 주자를 베이스에 남기지 않는다.
* **핸디캡**: 이닝이 끝날 때 점수 차가 `MATCH.HANDICAP_DIFF` 이상이면 뒤진 쪽 다음 이닝 공격 +1 (마지막 이닝 제외).
  기준값은 [미정] — 임시로 1000(한 타석 최대 점수). 봇 대 봇 200경기에서 1이닝(초+말) 평균 약 2150점, 이닝 종료 시 31% 발동.
* **동점 = 무승부.** 방은 하나, 선착 2명. 한쪽이 나가면 남은 쪽에 `OPPONENT_LEFT` 후 로비로.
* **연습 모드 = 역할 선택** (봇 수비 상대로 공격 / 봇 타자 상대로 수비). 봇은 확률로만 움직인다.
* 학습 단계(정답 화살표)는 [미정]이라 넣지 않았다. 유효 창에 들어온 위험 칸이 번쩍이는 것만 있다.

### 패킷

`{ t, p }` JSON.
`MATCH_START`(role, you, inning, half, halves, attacks, scores) · `AT_BAT_START`(seq, inning, half, outs, attackNo, attacksTotal, scores, costGrant) ·
`PITCH_SELECT`(seq, pitchNo, cardId(직구 null), start(타자 시점 칸), speedMul, timeout) · `BAT_RESULT`(seq, pitchNo, result, reason, outcome, strikes, balls, lives) ·
`RUN_STATE`(스냅숏) · `CARD_PLAY`(seq, cardId, zones, spawnTime) · `DODGE_SUCCESS`(seq, costGained) ·
`AT_BAT_END`(공격자 → 호스트 → **양쪽**: out, reason, bases, walk, batGrade, livesStart, livesLeft, stats + gained, outs, scores) ·
`INNING_CHANGE`(scores, role, inning, half, handicap(공격 +1 받는 자리 번호 / null)) · `GAME_OVER`(winner(무승부 null), scores) ·
`WAITING` · `ROOM_FULL` · `OPPONENT_LEFT`.
타석 패킷은 `seq` 로 이전 타석의 늦은 패킷을 버리고, 투구는 `pitchNo` 로 같은 타석 안의 순서를 맞춘다.

여전히 확정되지 않은 것: 기획서 v2 부록 C (핸디캡 기준값, 각종 [가안] 수치, 학습 단계 위치 등). 새 항목이 생기면 여기에 적는다.

## 만들지 않는 것

핵심 재미가 검증되기 전에 만들면 프로젝트가 비대해진다. 요청받지 않은 한 손대지 않는다.

실제 경기 규칙 · 볼/스트라이크 이상의 야구 규칙 · 정교한 투수·수비 AI(연습 봇은 확률 봇까지만) · 베이스 이동 AI · 다수 주자 ·
수비 포지셔닝 · 야구 스탯 · 선수 성장 · 커스터마이징 · 스킨 · 장비 · 상점 · 재화 · 뽑기 · 강화 · 랭킹 ·
인터넷 매칭·계정·여러 방·재접속 복구(LAN 방 하나까지만) · 스테이지 선택 · 실제 야구장 제작 · 복잡한 메타 시스템.
덱 편집은 v2 에서 **장애물 카드 6장 고르기**까지만 만든다.

`아이디어.md` 의 데드볼·PERFECT 가속 아이디어는 기획서에 들어가기 전까지 구현하지 않는다.

## 무엇을 먼저 검증하는가

**타격보다 주루 시스템 검증이 우선이다.** 가장 중요한 질문은 "4방향 대응 주루가 실제로 재미있는가?" 다.

우선순위: ① 공 좌상/우상 → 반대쪽 상체 회피 ② 수비수 좌하/우하 → 같은 쪽 다리 회피
③ 수비 카드 기습이 "읽고 반응하는" 심리전으로 느껴지는가 (색·경고 단서 포함).

v2 에서 욕심이 생기는 지점은 **생명 하나를 쥐고 3루를 도는 순간**(빗맞은 타구도 끝까지 안 맞으면 홈런)이고,
수비 쪽은 **필드 장애물 예고를 먼저 보고 카드를 얹는 순간**이다.
